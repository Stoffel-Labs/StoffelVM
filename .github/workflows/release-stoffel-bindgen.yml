name: Publish stoffel-bindgen

on:
  push:
    tags:
      - "stoffel-bindgen-v*.*.*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag, e.g. stoffel-bindgen-v0.2.0"
        required: true
        type: string

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    uses: ./.github/workflows/_publish-crate.yml
    with:
      crate: stoffel-bindgen
      tag: ${{ inputs.tag }}   # empty on tag-push; the reusable workflow falls back to the ref
    secrets:
      CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
